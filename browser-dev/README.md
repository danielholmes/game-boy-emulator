# Gebby Browser Dev Environment

A debugging tool in the browser for emulating the Game Boy. Currently just runs the BIOS.


## Running

```bash
yarn start
```

Now available at [http://localhost:3000](http://localhost:3000)
